name: CI

on:
  push:
    branches:
      - cao
  pull_request:
    branches:
      - main
      - cao

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r apps/local-sentinel/requirements.txt
          pip install -r apps/server/requirements.txt
          pip install pytest

      - name: Compile Python
        run: python -m compileall -q apps

      - name: Run tests
        env:
          PYTHONPATH: apps/local-sentinel:apps/server
        run: python -m pytest -q apps/local-sentinel/tests apps/server/tests
